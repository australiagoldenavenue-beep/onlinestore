# Database Configuration
# IMPORTANT: Replace [YOUR-PASSWORD] with your actual Supabase database password
# For direct connection (not through pgBouncer), use port 5432 instead of 6543
# Direct connection URL format:
DATABASE_URL="postgresql://postgres.otcjbuizvszevursgiuv:[YOUR-PASSWORD]@aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres?sslmode=require"

# For connection pooling (pgBouncer), use this instead:
# DATABASE_URL="postgresql://postgres.otcjbuizvszevursgiuv:[YOUR-PASSWORD]@aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"

# NextAuth Configuration
# Generate a random secret: openssl rand -base64 32
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Stripe Payment Configuration (Optional - for payment processing)
# Get these from https://dashboard.stripe.com/apikeys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""

# Resend Email Configuration (Optional - for email notifications)
# Get API key from https://resend.com/api-keys
RESEND_API_KEY=""

# Application Settings
NODE_ENV="development"
